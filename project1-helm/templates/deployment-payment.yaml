apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-payment
    projectName: {{ .Values.labels.projectName }}
  name: deploy-payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploy-payment
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: deploy-payment
    spec:
      containers:
      - image: {{ .Values.image.repo}}/project1-payment:{{ .Values.image.version}}
        name: project1-payment
        ports:
        - containerPort: 8080
        resources: {}
status: {}
