apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-shipping
    projectName: {{ .Values.labels.projectName }}
  name: deploy-shipping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploy-shipping
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: deploy-shipping
    spec:
      containers:
      - image: {{ .Values.image.repo}}/project1-shipping:{{ .Values.image.version }}
        name: project1-shipping
        ports:
        - containerPort: 8080
        resources: {}
status: {}
