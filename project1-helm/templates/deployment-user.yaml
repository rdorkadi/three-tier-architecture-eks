apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-user
    projectName: {{ .Values.labels.projectName }}
  name: deploy-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploy-user
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: deploy-user
    spec:
      containers:
      - image: {{ .Values.image.repo }}/project1-user:{{ .Values.image.version }}
        name: project1-user
        ports:
        - containerPort: 8080
        resources: {}
status: {}
